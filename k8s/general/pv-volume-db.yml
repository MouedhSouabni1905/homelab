apiVersion: v1
kind: PersistentVolume
metadata:
  name: bunkerweb-mysql-volume
  labels:
    type: local
spec:
  storageClassName: rp-db
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/mysql"

