# Joplin
After the containers have started running and you checked the `/api/ping` route to verify server health, you need to access the web interface of the server. For that, the default credentials are `admin@localhost` as email and `admin` as password.
